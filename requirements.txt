# Core dependencies
numpy>=1.24.0
PyYAML>=6.0

# Model merging
mergekit>=0.0.3
torch>=2.0.0
transformers>=4.30.0
peft>=0.5.0  # For LoRA model merging

# Evaluation
lm-eval>=0.4.0

# Optional: For GPU acceleration
# nvidia-cuda-runtime-cu12
# nvidia-cuda-nvrtc-cu12

# Development
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
